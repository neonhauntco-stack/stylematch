'use client';import React from 'react';import { auth, provider } from '../lib/firebase';import { signInWithPopup, signOut } from 'firebase/auth';export function LoginButton({ user }:{ user:any }){if(user){return(<button className="text-xs tracking-wider rounded-none border border-black bg-transparent text-black px-4 py-2" onClick={()=>signOut(auth)}>Logout</button>);}return(<button className="text-xs tracking-wider rounded-none border border-black bg-black text-white px-4 py-2" onClick={()=>signInWithPopup(auth,provider)}>Login</button>);}