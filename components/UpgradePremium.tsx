'use client';import React,{useState} from 'react';export function UpgradePremium(){const[loading,setLoading]=useState(false);const handleUpgrade=async()=>{setLoading(true);const res=await fetch('/api/stripe/checkout',{method:'POST'});const data=await res.json();if(data?.url) window.location.href=data.url;setLoading(false);};return(<button onClick={handleUpgrade} disabled={loading} className="border border-black rounded-none px-4 py-2 text-sm uppercase tracking-widest">{loading?'Processing…':'Upgrade to Premium (€9.99/mo)'}</button>);}